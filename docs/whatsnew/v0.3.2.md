# v0.3.2

## 本版新加了甚麼

`doctor` 和 `init` 會顯示「項目狀態速覽」，包括工具版本、項目記錄版本、npm 最新版本、上次收工時間，以及項目首次安裝距今多久。`upgrade` 完成後也會直接顯示本版重點，讓使用者不必再另外查找更新說明。

## 對你已有檔案的影響

本版只改善工具提示與狀態顯示，不會改動既有項目內容，也不會改變已安裝的 Kit 文件結構。

## 建議下一步

執行 `doctor` 檢查項目健康，並留意輸出中的「項目狀態速覽」。
